name: MEAN App CI/CD Pipeline

on:
  push:
    branches:
      - main

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      - name: Build and push backend image
        uses: docker/build-push-action@v6
        with:
          context: ./crud-dd-task-mean-app/backend
          file: ./crud-dd-task-mean-app/backend/Dockerfile
          push: true
          tags: ${{ secrets.DOCKER_USERNAME }}/crud-mean-backend:latest

      - name: Build and push frontend image
        uses: docker/build-push-action@v6
        with:
          context: ./crud-dd-task-mean-app/frontend
          file: ./crud-dd-task-mean-app/frontend/Dockerfile
          push: true
          tags: ${{ secrets.DOCKER_USERNAME }}/crud-mean-frontend:latest

      - name: Deploy to Ubuntu VM
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.VM_HOST }}
          username: ${{ secrets.VM_USER }}
          key: ${{ secrets.VM_SSH_KEY }}
          script: |
            cd ~/crud-dd-task-mean-app
            docker compose pull
            docker compose up -d